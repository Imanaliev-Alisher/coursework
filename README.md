# AIS "Class Schedule" - Автоматизированная информационная система управления расписанием

[![Django](https://img.shields.io/badge/Django-5.2-green.svg)](https://www.djangoproject.com/)
[![Python](https://img.shields.io/badge/Python-3.12-blue.svg)](https://www.python.org/)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue.svg)](https://www.docker.com/)
[![Tests](https://img.shields.io/badge/Tests-104%20passed-success.svg)](./TESTING.md)
[![Coverage](https://img.shields.io/badge/Coverage-80%25-brightgreen.svg)](./htmlcov/index.html)

## 📋 Описание проекта

Система управления расписанием занятий для университета. REST API построен на Django с использованием Docker для контейнеризации.

### Основные возможности

- 👥 **Управление пользователями**: студенты, преподаватели, администраторы
- 🏢 **Управление зданиями и аудиториями**
- 📚 **Управление предметами и типами занятий**
- 📅 **Динамическое расписание** с поддержкой четных/нечетных недель
- 🔄 **Переопределение расписания** для отдельных дат
- 📊 **Экспорт расписания** в PDF и Excel
- 🔐 **JWT аутентификация**
- 📖 **Автоматическая документация API** (Swagger/ReDoc)

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Git

### Установка и запуск

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd coursework
```

2. **Создание файла окружения**
```bash
cp .env.example .env
```

Отредактируйте `.env` при необходимости (по умолчанию настроен для разработки).

3. **Запуск проекта**
```bash
docker compose up -d
```

4. **Применение миграций**
```bash
docker compose exec django python manage.py migrate
```

5. **Создание суперпользователя**
```bash
docker compose exec django python manage.py createsuperuser
```

6. **Заполнение тестовыми данными (опционально)**
```bash
docker compose exec django python manage.py populate_db
```

Эта команда создаст:
- 3 администратора
- 15 преподавателей
- 30 студентов (3 группы по 10 человек)
- 10 предметов
- 2 здания с 30 аудиториями

### Проверка работы

Проект доступен по адресам:
- **API**: http://localhost:8000/api/v1/
- **Swagger документация**: http://localhost:8000/api/docs/
- **ReDoc документация**: http://localhost:8000/api/redoc/
- **Админ-панель**: http://localhost:8000/admin/

## 🏗️ Архитектура

### Технологический стек

**Backend:**
- Django 5.2
- Django REST Framework 3.15.2
- PostgreSQL
- Redis
- Celery (фоновые задачи)
- Gunicorn + Uvicorn (ASGI)

**Дополнительно:**
- JWT аутентификация (Simple JWT)
- drf-spectacular (документация API)
- Jazzmin (улучшенная админка)
- ReportLab (генерация PDF)
- OpenPyXL (генерация Excel)

### Структура проекта

```
coursework/
├── apps/                      # Django приложения
│   ├── users/                # Пользователи (студенты, преподаватели)
│   ├── buildings/            # Здания и аудитории
│   ├── groups/               # Учебные группы
│   └── studies/              # Предметы и расписание
├── config/                   # Настройки Django
├── compose/                  # Docker конфигурация
│   ├── django/              # Django Dockerfile и скрипты
│   ├── nginx/               # Nginx конфигурация
│   └── postgres/            # PostgreSQL Dockerfile
├── staticfiles/             # Статические файлы (собираются автоматически)
├── htmlcov/                 # Отчеты о покрытии тестами
├── docker-compose.yml       # Docker Compose конфигурация
├── manage.py                # Django management script
├── pytest.ini               # Конфигурация pytest
└── pyproject.toml           # Python зависимости (Poetry)
```

### Модель данных

**Пользователи (apps/users/):**
- User (расширенная модель с ролями: STUDENT, TEACHER, STAFF)
- Дополнительные поля для преподавателей (кафедра, телефон, кабинет)

**Здания (apps/buildings/):**
- Buildings (корпуса с адресами)
- Audiences (аудитории с типами и этажами)
- AudiencesTypes (типы аудиторий)

**Группы (apps/groups/):**
- StudyGroups (учебные группы с факультетом и курсом)
- Связь ManyToMany со студентами

**Предметы и расписание (apps/studies/):**
- Subjects (предметы)
- Schedule (расписание с днями недели и временными слотами)
- TimeSlot (временные интервалы пар)
- Day (дни недели)
- SubjectsTypes (типы занятий: лекция, практика)
- ScheduleOverride (переопределения расписания для конкретных дат)

## 📡 API Endpoints

### Аутентификация
```
POST   /api/v1/auth/token/          # Получить токен
POST   /api/v1/auth/token/refresh/  # Обновить токен
POST   /api/v1/auth/token/verify/   # Проверить токен
```

### Пользователи
```
GET    /api/v1/users/               # Список пользователей (admin)
POST   /api/v1/users/               # Создать пользователя
GET    /api/v1/users/{id}/          # Детали пользователя
PUT    /api/v1/users/{id}/          # Обновить пользователя
DELETE /api/v1/users/{id}/          # Удалить пользователя (admin)
GET    /api/v1/users/me/            # Текущий пользователь
POST   /api/v1/users/change-password/  # Сменить пароль
GET    /api/v1/users/students/      # Список студентов
GET    /api/v1/users/teachers/      # Список преподавателей
```

### Здания и аудитории
```
GET    /api/v1/buildings/           # Список зданий
POST   /api/v1/buildings/           # Создать здание (admin)
GET    /api/v1/buildings/{id}/      # Детали здания
GET    /api/v1/audiences/           # Список аудиторий
POST   /api/v1/audiences/           # Создать аудиторию (admin)
GET    /api/v1/audience-types/      # Типы аудиторий
```

### Учебные группы
```
GET    /api/v1/study-groups/        # Список групп
POST   /api/v1/study-groups/        # Создать группу (admin)
GET    /api/v1/study-groups/{id}/   # Детали группы
PUT    /api/v1/study-groups/{id}/   # Обновить группу (admin)
```

### Предметы и расписание
```
GET    /api/v1/subjects/            # Список предметов
POST   /api/v1/subjects/            # Создать предмет (admin)
GET    /api/v1/subjects/{id}/       # Детали предмета
GET    /api/v1/subjects/{id}/pdf/   # Экспорт расписания в PDF
GET    /api/v1/subjects/{id}/excel/ # Экспорт расписания в Excel
GET    /api/v1/schedules/           # Список расписаний
GET    /api/v1/timeslots/           # Временные слоты
GET    /api/v1/schedule-overrides/  # Переопределения расписания
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
docker compose exec django pytest apps/users/tests.py apps/buildings/tests.py apps/groups/tests.py apps/studies/tests.py

# Краткий вывод
docker compose exec django pytest apps/users/tests.py apps/buildings/tests.py apps/groups/tests.py apps/studies/tests.py -q

# С покрытием кода
docker compose exec django pytest apps/users/tests.py apps/buildings/tests.py apps/groups/tests.py apps/studies/tests.py --cov=apps --cov-report=html

# Конкретный модуль
docker compose exec django pytest apps/users/tests.py -v
```

### Статистика тестов

- **Всего тестов**: 104
- **Успешных**: 104
- **Покрытие кода**: 80%

**По модулям:**
- apps/users: 38 тестов (87% покрытие)
- apps/buildings: 25 тестов (94% покрытие)
- apps/groups: 18 тестов (86% покрытие)
- apps/studies: 23 теста (72% покрытие)

HTML отчет: `htmlcov/index.html`

## 🔧 Разработка

### Полезные команды

```bash
# Логи контейнеров
docker compose logs -f

# Логи конкретного сервиса
docker compose logs -f django

# Перезапуск сервиса
docker compose restart django

# Django shell
docker compose exec django python manage.py shell

# Создание миграций
docker compose exec django python manage.py makemigrations

# Применение миграций
docker compose exec django python manage.py migrate

# Сбор статических файлов
docker compose exec django python manage.py collectstatic --noinput

# Проверка конфигурации
docker compose exec django python manage.py check
```

### Работа с БД

```bash
# Вход в PostgreSQL
docker compose exec postgres psql -U debug coursework

# Бэкап БД
docker compose exec postgres backup

# Восстановление БД
docker compose exec postgres restore <backup_file>
```

### Celery задачи

```bash
# Просмотр Celery воркеров
docker compose logs celeryworker

# Просмотр Celery beat (планировщик)
docker compose logs celerybeat
```

## 📝 Права доступа

### Роли пользователей

1. **STUDENT (Студент)**
   - Просмотр своего профиля
   - Просмотр расписания своих групп
   - Просмотр информации о преподавателях

2. **TEACHER (Преподаватель)**
   - Все права студента
   - Просмотр своих предметов
   - Просмотр списка студентов в группах

3. **STAFF (Администратор)**
   - Полный доступ ко всем операциям
   - Управление пользователями
   - Управление расписанием
   - Управление зданиями и аудиториями

## 🐛 Известные проблемы

1. **Предупреждение о STATICFILES_DIRS** - можно игнорировать, статические файлы собираются корректно
2. **DeprecationWarning от factory-boy** - библиотека тестирования, не влияет на продакшн

## 📚 Дополнительная документация

- [Инструкции по тестированию](./TESTING.md)
- [Swagger UI](http://localhost:8000/api/docs/) - интерактивная документация API
- [ReDoc](http://localhost:8000/api/redoc/) - альтернативная документация API

## 🤝 Контакты

По вопросам обращайтесь к разработчикам проекта.

## 📄 Лицензия

Проект разработан в рамках курсовой работы.
